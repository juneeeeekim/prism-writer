# 🎙️ LLM 모델 관리 시스템 개선 - 아이디어 회의록

> **회의 일시**: 2025-12-21 21:00  
> **회의 목적**: LLM 모델 추가/스위칭을 쉽게 만들기 위한 아이디어 수집  
> **참석자**: 시니어 백엔드 개발자, AI/ML 엔지니어, 프론트엔드 개발자, DevOps 엔지니어, UX 설계자

---

## 📍 현재 상태 분석

### 현재 구조의 문제점

| 문제점                | 설명                                | 영향받는 파일                         |
| --------------------- | ----------------------------------- | ------------------------------------- |
| **하드코딩된 모델명** | 모델명이 코드에 직접 기재됨         | `client.ts`, `rag.ts`                 |
| **Provider 종속**     | Gemini 전용 코드로 작성됨           | `client.ts` (GoogleGenerativeAI)      |
| **분산된 설정**       | 모델 설정이 여러 파일에 흩어져 있음 | `client.ts`, `rag.ts`, `telemetry.ts` |
| **동적 전환 불가**    | 런타임에 모델 전환이 어려움         | 전체 시스템                           |

### 현재 코드 구조

```
frontend/src/lib/llm/
├── client.ts      # Gemini 전용 클라이언트 (하드코딩)
├── parser.ts      # 응답 파싱
└── prompts.ts     # 프롬프트 빌더

frontend/src/types/
└── rag.ts         # ROUTER_CONFIGS에 모델명 하드코딩
```

---

## 💡 아이디어 수집

---

### 👨‍💻 시니어 백엔드 개발자

#### 💡 아이디어 1: Provider Abstraction Layer (추상화 레이어)

**개념**: 모든 LLM Provider를 통일된 인터페이스로 추상화

```typescript
// 🔧 제안 구조: frontend/src/lib/llm/providers/base.ts
interface LLMProvider {
  name: string;
  generateText(prompt: string, options: LLMOptions): Promise<LLMResponse>;
  generateStream(
    prompt: string,
    options: LLMOptions
  ): AsyncGenerator<LLMStreamChunk>;
  isAvailable(): boolean;
  getSupportedModels(): ModelInfo[];
}
```

**장점**:

- 새 Provider 추가 시 인터페이스만 구현하면 됨
- 기존 코드 수정 최소화
- 테스트 용이성 (Mock Provider 가능)

**구현 예시**:

```typescript
// frontend/src/lib/llm/providers/gemini.ts
class GeminiProvider implements LLMProvider {
  name = "gemini";
  // ... 구현
}

// frontend/src/lib/llm/providers/openai.ts
class OpenAIProvider implements LLMProvider {
  name = "openai";
  // ... 구현
}

// frontend/src/lib/llm/providers/anthropic.ts
class AnthropicProvider implements LLMProvider {
  name = "anthropic";
  // ... 구현
}
```

---

### 🤖 AI/ML 엔지니어

#### 💡 아이디어 2: Model Registry (모델 레지스트리)

**개념**: 모든 모델 정보를 중앙 집중화된 설정 파일로 관리

```typescript
// 🔧 제안 파일: frontend/src/config/models.ts
export const MODEL_REGISTRY: Record<string, ModelConfig> = {
  // Gemini 모델
  "gemini-2.0-flash": {
    provider: "gemini",
    displayName: "Gemini 2.0 Flash",
    capabilities: ["text-generation", "streaming"],
    costPerInputToken: 0.000075,
    costPerOutputToken: 0.0003,
    maxTokens: 8192,
    isDefault: true,
  },
  "gemini-3-pro-preview": {
    provider: "gemini",
    displayName: "Gemini 3 Pro Preview",
    capabilities: ["text-generation", "streaming", "reasoning"],
    costPerInputToken: 0.00125,
    costPerOutputToken: 0.005,
    maxTokens: 32768,
    tier: "premium",
  },

  // OpenAI 모델 (추가 예정)
  "gpt-4o": {
    provider: "openai",
    displayName: "GPT-4o",
    capabilities: ["text-generation", "vision", "streaming"],
    costPerInputToken: 0.005,
    costPerOutputToken: 0.015,
    maxTokens: 128000,
    tier: "premium",
  },

  // Anthropic 모델 (추가 예정)
  "claude-3-5-sonnet": {
    provider: "anthropic",
    displayName: "Claude 3.5 Sonnet",
    capabilities: ["text-generation", "streaming"],
    costPerInputToken: 0.003,
    costPerOutputToken: 0.015,
    maxTokens: 200000,
    tier: "premium",
  },
};
```

**장점**:

- 모델 추가 시 설정 파일만 수정
- 비용 계산 자동화
- UI에서 동적으로 모델 목록 표시 가능

---

#### 💡 아이디어 3: 임베딩 모델 분리 관리

**개념**: 텍스트 생성 모델과 임베딩 모델을 별도로 관리

```typescript
// 🔧 제안 파일: frontend/src/config/embedding-models.ts
export const EMBEDDING_REGISTRY: Record<string, EmbeddingConfig> = {
  "text-embedding-004": {
    provider: "gemini",
    dimensions: 768,
    costPerToken: 0.00001,
    isDefault: true,
  },
  "text-embedding-3-small": {
    provider: "openai",
    dimensions: 1536,
    costPerToken: 0.00002,
  },
};
```

**⚠️ 주의**: 임베딩 모델 변경 시 벡터 차원이 바뀌므로 DB 마이그레이션 필요!

---

### 👩‍💻 프론트엔드 개발자

#### 💡 아이디어 4: 관리자 UI 대시보드

**개념**: 코드 수정 없이 UI에서 모델 관리

```
┌─────────────────────────────────────────────────────┐
│  🔧 LLM 모델 관리                                    │
├─────────────────────────────────────────────────────┤
│  [현재 활성 모델]                                    │
│  ┌─────────────────────────────────────────────────┐│
│  │ 🟢 gemini-2.0-flash (기본)                      ││
│  │    Provider: Gemini | 비용: $0.075/1K tokens     ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  [사용 가능 모델]                                    │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ │
│  │ gemini-3-pro │ │   gpt-4o     │ │ claude-3.5   │ │
│  │  [활성화]    │ │  [활성화]    │ │  [활성화]    │ │
│  └──────────────┘ └──────────────┘ └──────────────┘ │
│                                                      │
│  [모드별 설정]                                       │
│  ┌─────────────────────────────────────────────────┐│
│  │ cheap:    ▼ gemini-2.0-flash                    ││
│  │ standard: ▼ gemini-2.0-flash                    ││
│  │ strict:   ▼ gemini-3-pro-preview                ││
│  └─────────────────────────────────────────────────┘│
│                                                      │
│  [💾 저장] [↩️ 초기화]                               │
└─────────────────────────────────────────────────────┘
```

**장점**:

- 비개발자도 모델 전환 가능
- A/B 테스트 용이
- 실시간 모니터링 연동 가능

---

### 🛠️ DevOps 엔지니어

#### 💡 아이디어 5: 환경 변수 + 설정 파일 분리

**개념**: Provider 별 API 키는 환경 변수, 모델 설정은 설정 파일

```bash
# .env.local (API 키만)
GOOGLE_API_KEY=AIzaSy...
OPENAI_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-ant-...

# 모델 활성화 플래그
ENABLED_PROVIDERS=gemini,openai
DEFAULT_MODEL=gemini-2.0-flash
```

```typescript
// 🔧 제안 파일: frontend/src/config/llm.config.ts
export function getEnabledProviders(): string[] {
  return (process.env.ENABLED_PROVIDERS || "gemini").split(",");
}

export function getDefaultModel(): string {
  return process.env.DEFAULT_MODEL || "gemini-2.0-flash";
}
```

**장점**:

- 배포 환경별 다른 모델 사용 가능 (dev: cheap, prod: premium)
- 보안 (API 키 분리)
- CI/CD 파이프라인에서 동적 설정 가능

---

#### 💡 아이디어 6: Fallback Chain (장애 대응)

**개념**: Primary 모델 실패 시 자동으로 Fallback 모델로 전환

```typescript
// 🔧 제안 로직
const FALLBACK_CHAIN = {
  "gemini-3-pro-preview": ["gemini-2.0-flash", "gpt-4o"],
  "gpt-4o": ["gpt-4o-mini", "gemini-2.0-flash"],
  "claude-3-5-sonnet": ["claude-3-haiku", "gemini-2.0-flash"],
};

async function generateWithFallback(prompt: string, primaryModel: string) {
  const chain = [primaryModel, ...(FALLBACK_CHAIN[primaryModel] || [])];

  for (const model of chain) {
    try {
      return await getProvider(model).generateText(prompt);
    } catch (error) {
      console.warn(`${model} 실패, 다음 모델 시도...`);
      continue;
    }
  }
  throw new Error("모든 모델 실패");
}
```

**장점**:

- 서비스 안정성 향상
- 특정 Provider 장애 시에도 서비스 지속 가능

---

### 🎨 UX 설계자

#### 💡 아이디어 7: 사용자별 모델 선택 기능

**개념**: 사용자가 직접 사용할 모델을 선택할 수 있게 함

```
┌─────────────────────────────────────┐
│  ⚙️ AI 설정                         │
├─────────────────────────────────────┤
│  응답 품질 설정:                     │
│                                      │
│  ○ 빠른 응답 (gemini-2.0-flash)     │
│     → 빠르고 경제적                  │
│                                      │
│  ● 균형 (기본값)                     │
│     → 속도와 품질의 균형             │
│                                      │
│  ○ 최고 품질 (gemini-3-pro)          │
│     → 느리지만 정확함                │
│                                      │
│  [저장]                              │
└─────────────────────────────────────┘
```

**장점**:

- 사용자 맞춤형 경험
- Premium 사용자에게 고급 모델 제공 가능

---

## 🏗️ 제안 아키텍처 (종합)

```
┌──────────────────────────────────────────────────────────────────┐
│                        Application Layer                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────────────┐│
│  │  RAG Module  │  │  Evaluate    │  │  Other AI Features       ││
│  └──────┬───────┘  └──────┬───────┘  └────────────┬─────────────┘│
│         │                 │                        │              │
│         └─────────────────┼────────────────────────┘              │
│                           ▼                                        │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                    LLM Gateway (NEW)                        │  │
│  │  ┌─────────────────────────────────────────────────────┐   │  │
│  │  │  Model Router  │  Fallback Chain  │  Usage Tracker  │   │  │
│  │  └─────────────────────────────────────────────────────┘   │  │
│  └────────────────────────────┬───────────────────────────────┘  │
│                               ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │                   Provider Layer (NEW)                      │  │
│  │  ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐   │  │
│  │  │  Gemini  │  │  OpenAI  │  │ Anthropic │  │  Custom  │   │  │
│  │  │ Provider │  │ Provider │  │ Provider  │  │ Provider │   │  │
│  │  └──────────┘  └──────────┘  └──────────┘  └──────────┘   │  │
│  └────────────────────────────────────────────────────────────┘  │
│                               ▼                                    │
│  ┌────────────────────────────────────────────────────────────┐  │
│  │               Model Registry (config/models.ts)             │  │
│  │   gemini-2.0-flash | gpt-4o | claude-3.5-sonnet | ...      │  │
│  └────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📁 제안 파일 구조

```
frontend/src/
├── config/
│   ├── models.ts              # [NEW] 모델 레지스트리
│   ├── embedding-models.ts    # [NEW] 임베딩 모델 레지스트리
│   └── llm.config.ts          # [NEW] LLM 글로벌 설정
│
├── lib/llm/
│   ├── gateway.ts             # [NEW] LLM Gateway (진입점)
│   ├── providers/
│   │   ├── base.ts            # [NEW] Provider 인터페이스
│   │   ├── gemini.ts          # [NEW] Gemini Provider
│   │   ├── openai.ts          # [NEW] OpenAI Provider
│   │   └── anthropic.ts       # [NEW] Anthropic Provider
│   ├── router.ts              # [MODIFY] Model Router
│   ├── fallback.ts            # [NEW] Fallback Chain
│   ├── client.ts              # [DEPRECATE] → gateway.ts로 대체
│   ├── parser.ts              # [KEEP] 유지
│   └── prompts.ts             # [KEEP] 유지
```

---

## ✅ 실행 우선순위

| 우선순위 | 아이디어             | 난이도   | 예상 공수 | 비고                    |
| -------- | -------------------- | -------- | --------- | ----------------------- |
| 🥇 P0    | Model Registry       | ⭐⭐     | 2시간     | 가장 기본, 먼저 해야 함 |
| 🥈 P1    | Provider Abstraction | ⭐⭐⭐   | 4시간     | Model Registry와 함께   |
| 🥉 P2    | 환경 변수 분리       | ⭐       | 1시간     | 보안 개선               |
| P3       | Fallback Chain       | ⭐⭐     | 2시간     | 안정성 개선             |
| P4       | 관리자 UI            | ⭐⭐⭐⭐ | 8시간     | 운영 편의성             |
| P5       | 사용자별 선택        | ⭐⭐⭐   | 4시간     | UX 개선                 |

---

## 📝 회의 결론

### 합의된 사항

1. **Model Registry + Provider Abstraction**을 함께 구현 (P0 + P1)
2. 환경 변수 분리로 보안 강화 (P2)
3. 이후 Fallback Chain과 관리자 UI는 점진적 도입

### 다음 단계

1. [ ] 상세 구현 체크리스트 작성
2. [ ] Model Registry 스키마 확정
3. [ ] Provider 인터페이스 설계 완료
4. [ ] 기존 코드 마이그레이션 계획 수립

---

> **회의록 작성**: AI 개발 어시스턴트  
> **다음 회의 예정**: 구현 체크리스트 검토
