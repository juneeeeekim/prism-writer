# LLM 중앙 관리 시스템 완전 전환 기술 문서

**문서 번호**: DEV-2026-0110-LLM-CENTRAL
**작성일**: 2026-01-10
**목적**: 모든 LLM API 사용을 중앙 관리 시스템으로 전환
**상태**: 📋 분석 완료 / 구현 대기

---

## 👥 참여 전문가 소개

### 🏆 Antigravity (기술 리더 / Tech Lead)

- **역할**: 프로젝트 총괄, 아키텍처 결정, 코드 리뷰
- **전문 분야**: Full-Stack Development, System Architecture
- **담당**: 전체 마이그레이션 계획 수립 및 실행 감독

### 🏗️ 아키텍처 전문가 (Architecture Expert)

- **역할**: 시스템 설계, 패턴 적용, 확장성 검토
- **전문 분야**: Software Architecture, Design Patterns
- **담당**: 중앙 관리 시스템 아키텍처 설계

### 🔧 백엔드 시니어 개발자 (Backend Senior)

- **역할**: API 구현, 데이터 흐름 설계
- **전문 분야**: Node.js, Next.js API Routes
- **담당**: 각 서비스 파일의 마이그레이션 구현

### 📊 DevOps 전문가 (DevOps Expert)

- **역할**: 배포, 모니터링, 롤백 전략
- **전문 분야**: Vercel, CI/CD, Feature Flags
- **담당**: 안전한 점진적 롤아웃 계획

---

## 📊 현재 상태 분석

### ✅ 중앙 관리 사용 중인 파일 (5개)

| 파일                       | 사용 컨텍스트                                                           | 상태    |
| -------------------------- | ----------------------------------------------------------------------- | ------- |
| `lib/rag/templateGates.ts` | `template.consistency`, `template.hallucination`, `template.regression` | ✅ 완료 |
| `lib/rag/ruleMiner.ts`     | `rule.mining`                                                           | ✅ 완료 |
| `lib/rag/reranker.ts`      | `rag.reranker`                                                          | ✅ 완료 |
| `lib/rag/exampleMiner.ts`  | `example.mining`                                                        | ✅ 완료 |
| `types/rag.ts`             | `premium.answer`                                                        | ✅ 완료 |

### ❌ 하드코딩된 파일 (마이그레이션 필요) - 10개

| 파일                               | 현재 모델                | 권장 컨텍스트              | 우선순위 |
| ---------------------------------- | ------------------------ | -------------------------- | -------- |
| `/api/suggest/route.ts`            | `gemini-1.5-flash`       | `suggest.completion`       | 🔴 높음  |
| `lib/rag/selfRAG.ts` (3곳)         | `gemini-1.5-flash`       | `rag.selfrag`              | 🔴 높음  |
| `lib/rag/rerank.ts`                | `gemini-1.5-flash`       | (이미 reranker.ts와 중복?) | 🟡 중간  |
| `lib/rag/agenticChunking.ts`       | `gemini-1.5-flash`       | `rag.chunking`             | 🟡 중간  |
| `lib/research/queryGenerator.ts`   | `gemini-1.5-flash`       | `research.query`           | 🔴 높음  |
| `lib/research/resultSummarizer.ts` | `gemini-1.5-flash`       | `research.summarize`       | 🔴 높음  |
| `lib/rag/patternExtractor.ts`      | `gemini-3-flash-preview` | `pattern.extraction`       | 🟡 중간  |
| `lib/judge/alignJudge.ts`          | `gemini-3-flash-preview` | `judge.align`              | 🔴 높음  |
| `lib/judge/holisticAdvisor.ts`     | `gemini-3-flash-preview` | `judge.holistic`           | 🔴 높음  |
| `/api/outline/route.ts`            | `gemini-3-flash-preview` | `outline.generation`       | 🟡 중간  |
| `lib/ocr/geminiVision.ts`          | `gemini-1.5-flash`       | `ocr.vision`               | 🟢 낮음  |

---

## 🏗️ 아키텍처 전문가 제안

### 현재 아키텍처 문제점

```ascii
┌─────────────────────────────────────────────────────────────────┐
│                     현재 상태 (분산형)                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐         │
│  │ suggest API  │   │   selfRAG    │   │ alignJudge   │         │
│  │ gemini-1.5   │   │ gemini-1.5   │   │ gemini-3     │         │
│  │  (하드코딩)   │   │  (하드코딩)   │   │  (하드코딩)   │         │
│  └──────────────┘   └──────────────┘   └──────────────┘         │
│         ↓                 ↓                   ↓                   │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                    문제점                                    │ │
│  │  • 모델 변경 시 여러 파일 수정 필요                           │ │
│  │  • 일관성 없는 모델 사용 (gemini-1.5 vs gemini-3)            │ │
│  │  • 오타/잘못된 모델 ID 발생 가능 (오늘 발생한 오류!)          │ │
│  │  • 비용 추적 어려움                                          │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

### 목표 아키텍처 (중앙 집중형)

```ascii
┌─────────────────────────────────────────────────────────────────┐
│                     목표 상태 (중앙 집중형)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────┐   ┌──────────────┐   ┌──────────────┐         │
│  │ suggest API  │   │   selfRAG    │   │ alignJudge   │         │
│  │ getModel()   │   │ getModel()   │   │ getModel()   │         │
│  │  (중앙 참조)  │   │  (중앙 참조)  │   │  (중앙 참조)  │         │
│  └──────┬───────┘   └───────┬──────┘   └──────┬───────┘         │
│         │                   │                  │                  │
│         └───────────────────┼──────────────────┘                  │
│                             ▼                                     │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │              llm-usage-map.ts (Single Source of Truth)       │ │
│  │  ┌────────────────────────────────────────────────────────┐ │ │
│  │  │ 'suggest.completion': { modelId: 'gemini-1.5-flash' }  │ │ │
│  │  │ 'rag.selfrag':        { modelId: 'gemini-1.5-flash' }  │ │ │
│  │  │ 'judge.align':        { modelId: 'gemini-3-flash' }    │ │ │
│  │  │ ...                                                     │ │ │
│  │  └────────────────────────────────────────────────────────┘ │ │
│  └─────────────────────────────────────────────────────────────┘ │
│                             │                                     │
│                             ▼                                     │
│  ┌─────────────────────────────────────────────────────────────┐ │
│  │                       장점                                   │ │
│  │  ✅ 모델 변경 = 한 파일만 수정                               │ │
│  │  ✅ 일관성 보장 (타입 안전)                                  │ │
│  │  ✅ 오타 방지 (TypeScript 자동완성)                          │ │
│  │  ✅ 비용 추적 용이 (한 곳에서 모든 사용처 확인)               │ │
│  └─────────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📋 마이그레이션 계획

### Phase 1: LLM Usage Map 확장 (새 컨텍스트 추가)

**파일**: `frontend/src/config/llm-usage-map.ts`

```typescript
// 추가할 컨텍스트 (LLMUsageContext 타입에 추가)
| 'suggest.completion'    // Shadow Writer 문장 완성
| 'rag.selfrag'           // Self-RAG 검증
| 'rag.chunking'          // Agentic Chunking
| 'research.query'        // Deep Scholar 쿼리 생성
| 'research.summarize'    // Deep Scholar 요약
| 'pattern.extraction'    // 패턴 추출
| 'judge.align'           // 개별 평가
| 'judge.holistic'        // 종합 평가
| 'outline.generation'    // 목차 생성
| 'ocr.vision'            // OCR 비전
```

### Phase 2: 개별 파일 마이그레이션

**예시 변경 (Before → After)**:

```typescript
// ❌ Before (하드코딩)
const response = await generateText(prompt, {
  model: "gemini-1.5-flash", // 하드코딩
  maxTokens: 100,
});

// ✅ After (중앙 관리)
import { getModelForUsage } from "@/config/llm-usage-map";

const response = await generateText(prompt, {
  model: getModelForUsage("suggest.completion"), // 중앙 관리
  maxTokens: 100,
});
```

### Phase 3: 검증 및 배포

1. **타입 체크**: `npx tsc --noEmit`
2. **빌드 검증**: `npm run build`
3. **단위 테스트**: 각 LLM 호출 정상 동작 확인
4. **점진적 배포**: Feature Flag로 롤백 가능하게

---

## 🎯 예상 효과

| 항목                      | Before   | After            |
| ------------------------- | -------- | ---------------- |
| 모델 변경 시 수정 파일 수 | 10+ 파일 | 1 파일           |
| 모델 ID 오타 가능성       | 높음     | 없음 (타입 안전) |
| 전체 LLM 사용 현황 파악   | 어려움   | 즉시 가능        |
| 비용 추적                 | 어려움   | 중앙에서 가능    |

---

## ⏱️ 작업 예상 시간

| Phase    | 작업 내용              | 예상 시간   |
| -------- | ---------------------- | ----------- |
| Phase 1  | 타입 및 매핑 확장      | 30분        |
| Phase 2  | 10개 파일 마이그레이션 | 2시간       |
| Phase 3  | 검증 및 배포           | 1시간       |
| **총계** |                        | **3.5시간** |

---

## ✅ 결론 및 권장사항

1. **실현 가능**: 모든 하드코딩된 LLM 사용처를 중앙 관리로 전환 가능합니다.
2. **권장**: 즉시 마이그레이션을 진행하여 향후 모델 변경 시 유지보수 비용을 대폭 절감하세요.
3. **안전성**: 기존 동작은 유지되며, 모델 ID만 중앙에서 가져오도록 변경됩니다.

---

**다음 단계**: 사용자 승인 후 Phase 1부터 순차적으로 진행합니다.

**작성자**: Antigravity (Tech Lead) + 아키텍처 전문가 + 백엔드 시니어 개발자
