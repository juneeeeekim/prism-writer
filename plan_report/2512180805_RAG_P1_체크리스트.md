# 📋 RAG 파이프라인 P1 체크리스트

> **운영 안정화 - Reviewer 모델 / Model Router / Telemetry / Evidence Pack**

---

## 📌 문서 정보

- **작성일**: 2025-12-18
- **선행 조건**: [2512180805*RAG_P0*체크리스트.md](./2512180805_RAG_P0_체크리스트.md) 완료 필수
- **기반 문서**: [2512180737*RAG*파이프라인*재설계*분석.md](./2512180737_RAG_파이프라인_재설계_분석.md)
- **담당**: 시니어 개발자 (리드), 주니어 개발자 (구현), UX/UI 전문가 (UI 검토)
- **예상 소요**: 2주

---

## 🟡 Phase 1: Reviewer 모델 (검토 단계)

### 📍 영향받을 수 있는 기존 기능

- [ ] LLM API 응답 처리 (`api/llm/route.ts`)
- [ ] 결과 표시 UI
- [ ] P0의 Judge Contract

### 1.1 Reviewer 결과 타입 정의

- [ ] **파일**: `frontend/src/types/rag.ts`
- [ ] **이전 항목 연결**: P0의 `JudgeResult` 타입과 함께 사용
- [ ] Reviewer 인터페이스 정의:

  ```typescript
  export type ReviewBadge = "✅" | "⚠️" | "⛔";

  export interface ReviewResult {
    badge: ReviewBadge;
    confidence: number; // 0-1
    issues?: string[];
    reasoning: string;
  }
  ```

  - 🔍 품질: 명확한 유니온 타입 (`ReviewBadge`)

### 1.2 Reviewer 프롬프트 생성 함수

- [ ] **파일**: `frontend/src/lib/rag/reviewerPrompt.ts` (신규 생성)
- [ ] **이전 항목 연결**: 1.1의 타입 사용
- [ ] 검토 프롬프트 생성:
  ```typescript
  export function buildReviewerPrompt(
    answer: string,
    evidenceChunks: string[],
    judgeResult: JudgeResult
  ): string;
  ```
  - 🔍 품질: 근거와 답변 일치 여부 검토 지시
  - 🔍 품질: 환각 감지 지시문 포함

### 1.3 Reviewer 응답 파서

- [ ] **파일**: `frontend/src/lib/rag/reviewerParser.ts` (신규 생성)
- [ ] **이전 항목 연결**: 1.1의 `ReviewResult` 타입 사용
- [ ] Reviewer 응답 파싱:
  ```typescript
  export function parseReviewerResponse(response: string): ReviewResult | null;
  ```
  - 🔍 품질: 에러 처리 - 파싱 실패 시 기본 `⚠️` 반환
  - 🔍 품질: 신뢰도 임계값 기반 배지 결정

### 1.4 Reviewer 통합 (LLM API)

- [ ] **파일**: `frontend/src/app/api/llm/route.ts`
- [ ] **이전 항목 연결**: 1.2, 1.3 함수 import, P0의 Judge 결과
- [ ] Judge 후 Reviewer 호출:
  ```typescript
  // Judge 완료 후
  const reviewPrompt = buildReviewerPrompt(answer, chunks, judgeResult);
  const reviewResponse = await callLLM(reviewPrompt, { model: REVIEW_MODEL });
  const reviewResult = parseReviewerResponse(reviewResponse);
  ```
  - 🔍 품질: 별도 모델로 호출 (Answer 모델과 분리)

### 1.5 Reviewer 배지 UI 컴포넌트

- [ ] **파일**: `frontend/src/components/rag/ReviewBadge.tsx` (신규 생성)
- [ ] **이전 항목 연결**: 1.4의 `reviewResult` 사용
- [ ] 검토 배지 컴포넌트:

  ```tsx
  interface ReviewBadgeProps {
    result: ReviewResult;
  }

  export function ReviewBadge({ result }: ReviewBadgeProps) {
    const badgeStyles = {
      "✅": "bg-green-100 text-green-800",
      "⚠️": "bg-yellow-100 text-yellow-800",
      "⛔": "bg-red-100 text-red-800",
    };
    return (
      <span
        className={badgeStyles[result.badge]}
        aria-label={`검토 결과: ${result.badge}`}
        role="status"
      >
        {result.badge}
      </span>
    );
  }
  ```

  - 🔍 품질: 접근성 - `aria-label`, `role="status"`
  - 🔍 품질: 시각적 구분 - 색상 코딩

### ✅ Phase 1 검증 체크리스트

- [ ] **Syntax 오류 확인**: `npm run build` 성공
- [ ] **브라우저 테스트**:
  - [ ] 질문 → Judge → Reviewer → 배지 표시
  - [ ] 근거 일치 → ✅ 표시
  - [ ] 근거 부족 → ⚠️ 표시
  - [ ] 불일치 → ⛔ 표시
- [ ] **기존 기능 정상 동작**:
  - [ ] P0 기능 모두 정상

---

## 🟡 Phase 2: Model Router (3모드)

### 📍 영향받을 수 있는 기존 기능

- [ ] LLM API 호출
- [ ] 비용 관리 (`costGuard.ts`)
- [ ] Phase 1의 Reviewer

### 2.1 Router 설정 타입 정의

- [ ] **파일**: `frontend/src/types/rag.ts`
- [ ] **이전 항목 연결**: Phase 1의 타입들과 함께
- [ ] Router 설정 인터페이스:

  ```typescript
  export type RouterMode = "cheap" | "standard" | "strict";

  export interface RouterConfig {
    mode: RouterMode;
    answerModel: string;
    reviewerModel: string | null; // null = off
    maxTokens: number;
    timeout: number;
  }

  export const ROUTER_CONFIGS: Record<RouterMode, RouterConfig> = {
    cheap: {
      mode: "cheap",
      answerModel: "gpt-4o-mini",
      reviewerModel: null,
      maxTokens: 1000,
      timeout: 10000,
    },
    standard: {
      mode: "standard",
      answerModel: "gpt-4o-mini",
      reviewerModel: "gpt-4o-mini",
      maxTokens: 2000,
      timeout: 15000,
    },
    strict: {
      mode: "strict",
      answerModel: "gpt-4o",
      reviewerModel: "gpt-4o",
      maxTokens: 4000,
      timeout: 30000,
    },
  };
  ```

  - 🔍 품질: 타입 안전성 - `as const` 또는 `Record` 사용

### 2.2 Model Router 함수 생성

- [ ] **파일**: `frontend/src/lib/rag/modelRouter.ts` (신규 생성)
- [ ] **이전 항목 연결**: 2.1의 설정 사용
- [ ] 모드 기반 라우팅:

  ```typescript
  export function getRouterConfig(mode: RouterMode = "standard"): RouterConfig {
    return ROUTER_CONFIGS[mode];
  }

  export function selectModel(
    step: "answer" | "reviewer",
    config: RouterConfig
  ): string | null {
    if (step === "answer") return config.answerModel;
    return config.reviewerModel;
  }
  ```

  - 🔍 품질: 로깅 - 라우팅 결정 기록

### 2.3 LLM API에 Router 적용

- [ ] **파일**: `frontend/src/app/api/llm/route.ts`
- [ ] **이전 항목 연결**: 2.2의 Router 함수 import
- [ ] 요청에서 모드 받아 라우팅:

  ```typescript
  const mode = (body.mode as RouterMode) || "standard";
  const config = getRouterConfig(mode);

  // Answer 호출
  const answerModel = selectModel("answer", config);
  const answer = await callLLM(prompt, { model: answerModel });

  // Reviewer 호출 (모드에 따라)
  const reviewerModel = selectModel("reviewer", config);
  if (reviewerModel) {
    const review = await callLLM(reviewPrompt, { model: reviewerModel });
  }
  ```

  - 🔍 품질: 타임아웃 적용

### 2.4 프론트엔드 모드 선택 UI

- [ ] **파일**: `frontend/src/components/rag/ModeSelector.tsx` (신규 생성)
- [ ] **이전 항목 연결**: 2.1의 `RouterMode` 타입
- [ ] 모드 선택 컴포넌트:

  ```tsx
  interface ModeSelectorProps {
    value: RouterMode;
    onChange: (mode: RouterMode) => void;
  }

  export function ModeSelector({ value, onChange }: ModeSelectorProps) {
    return (
      <select
        value={value}
        onChange={(e) => onChange(e.target.value as RouterMode)}
        aria-label="AI 모드 선택"
      >
        <option value="cheap">💰 경제 (빠름)</option>
        <option value="standard">⚖️ 표준</option>
        <option value="strict">🔒 정밀 (느림)</option>
      </select>
    );
  }
  ```

  - 🔍 품질: 접근성 - `aria-label`
  - 🔍 품질: 사용자 친화적 라벨

### ✅ Phase 2 검증 체크리스트

- [ ] **Syntax 오류 확인**: `npm run build` 성공
- [ ] **브라우저 테스트**:
  - [ ] cheap 모드 → 빠른 응답, Reviewer 없음
  - [ ] standard 모드 → Reviewer 표시
  - [ ] strict 모드 → 상세 Reviewer 표시
- [ ] **기존 기능 정상 동작**:
  - [ ] P0 + Phase 1 정상

---

## 🟡 Phase 3: Telemetry 강화 (run_id + 비용)

### 📍 영향받을 수 있는 기존 기능

- [ ] 모든 API 호출
- [ ] 비용 관리

### 3.1 Telemetry 타입 정의

- [ ] **파일**: `frontend/src/types/telemetry.ts` (신규 생성)
- [ ] 텔레메트리 인터페이스:
  ```typescript
  export interface TelemetryRecord {
    runId: string;
    userId: string;
    step: "search" | "rerank" | "answer" | "review" | "citation";
    startTime: number;
    endTime: number;
    latencyMs: number;
    modelId?: string;
    tokensIn: number;
    tokensOut: number;
    costEstimate: number;
    success: boolean;
    errorCode?: string;
  }
  ```
  - 🔍 품질: 필수/선택 필드 구분

### 3.2 Telemetry 유틸리티 함수

- [ ] **파일**: `frontend/src/lib/telemetry.ts` (신규 생성)
- [ ] **이전 항목 연결**: 3.1의 타입 사용
- [ ] run_id 생성 및 기록:

  ```typescript
  export function generateRunId(): string {
    return `run_${Date.now()}_${Math.random().toString(36).slice(2, 9)}`;
  }

  export async function logTelemetry(record: TelemetryRecord): Promise<void> {
    // Supabase에 저장
  }

  export function measureStep<T>(
    step: TelemetryRecord["step"],
    fn: () => Promise<T>
  ): Promise<{ result: T; telemetry: Partial<TelemetryRecord> }>;
  ```

  - 🔍 품질: 성능 측정 래퍼 함수

### 3.3 API에 Telemetry 통합

- [ ] **파일**: `frontend/src/app/api/llm/route.ts`
- [ ] **이전 항목 연결**: 3.2의 함수 import
- [ ] 각 단계에 텔레메트리 적용:

  ```typescript
  const runId = generateRunId();

  // 검색 단계
  const { result: searchResults, telemetry: searchTelemetry } =
    await measureStep("search", () => hybridSearch(query, options));
  await logTelemetry({ runId, ...searchTelemetry });

  // 답변 단계
  const { result: answer, telemetry: answerTelemetry } = await measureStep(
    "answer",
    () => callLLM(prompt)
  );
  await logTelemetry({ runId, ...answerTelemetry });
  ```

  - 🔍 품질: 비동기 로깅 (응답 지연 최소화)

### 3.4 DB 테이블 생성

- [ ] **파일**: `backend/migrations/019_telemetry_schema.sql`
- [ ] 텔레메트리 테이블:

  ```sql
  CREATE TABLE telemetry_logs (
    id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    run_id TEXT NOT NULL,
    user_id UUID REFERENCES auth.users(id),
    step TEXT NOT NULL,
    start_time BIGINT NOT NULL,
    end_time BIGINT NOT NULL,
    latency_ms INT NOT NULL,
    model_id TEXT,
    tokens_in INT DEFAULT 0,
    tokens_out INT DEFAULT 0,
    cost_estimate DECIMAL(10, 6) DEFAULT 0,
    success BOOLEAN NOT NULL,
    error_code TEXT,
    created_at TIMESTAMPTZ DEFAULT now()
  );

  CREATE INDEX idx_telemetry_run_id ON telemetry_logs(run_id);
  CREATE INDEX idx_telemetry_user_id ON telemetry_logs(user_id);
  ```

### ✅ Phase 3 검증 체크리스트

- [ ] **Syntax 오류 확인**: `npm run build` 성공
- [ ] **DB 확인**: `telemetry_logs` 테이블 생성됨
- [ ] **브라우저 테스트**:
  - [ ] 질문 → 각 단계 로그 저장됨
  - [ ] run_id로 전체 플로우 추적 가능
- [ ] **Supabase 대시보드**:
  - [ ] 로그 조회 가능
  - [ ] 비용 합산 가능

---

## 🟡 Phase 4: Evidence Pack 표준화

### 📍 영향받을 수 있는 기존 기능

- [ ] 검색 결과 처리
- [ ] Judge 입력
- [ ] 프론트엔드 근거 표시

### 4.1 Evidence Pack 타입 정의

- [ ] **파일**: `frontend/src/types/rag.ts`
- [ ] **이전 항목 연결**: 기존 `SearchResult` 확장
- [ ] Evidence Pack 인터페이스:

  ```typescript
  export interface EvidencePack {
    runId: string;
    rubricId?: string;
    items: EvidenceItem[];
    metadata: EvidenceMetadata;
  }

  export interface EvidenceItem {
    chunkId: string;
    documentId: string;
    content: string;
    spanOffsets: { start: number; end: number };
    sourceUri: string;
    namespace: string;
    docVersion: string;
    scoreComponents: {
      bm25: number;
      vector: number;
      rerank: number;
    };
  }

  export interface EvidenceMetadata {
    searchQuery: string;
    retrievalConfigId: string;
    embeddingModelId: string;
    totalCandidates: number;
    selectedCount: number;
    createdAt: string;
  }
  ```

  - 🔍 품질: 재현성 - 설정 ID 포함
  - 🔍 품질: 점수 분해 - 디버깅 용이

### 4.2 Evidence Pack 빌더 함수

- [ ] **파일**: `frontend/src/lib/rag/evidencePack.ts` (신규 생성)
- [ ] **이전 항목 연결**: 4.1의 타입, 검색 결과
- [ ] 검색 결과를 Evidence Pack으로 변환:
  ```typescript
  export function buildEvidencePack(
    runId: string,
    searchResults: SearchResult[],
    config: { query: string; retrievalConfigId: string }
  ): EvidencePack;
  ```
  - 🔍 품질: 불변성 - 원본 결과 수정 안함

### 4.3 Judge에 Evidence Pack 적용

- [ ] **파일**: `frontend/src/lib/rag/judgePrompt.ts`
- [ ] **이전 항목 연결**: 4.2의 `EvidencePack`
- [ ] Judge 프롬프트에 Evidence Pack 사용:
  ```typescript
  export function buildJudgePromptWithEvidence(
    query: string,
    evidencePack: EvidencePack,
    rubric?: string
  ): string;
  ```
  - 🔍 품질: 구조화된 컨텍스트 전달

### ✅ Phase 4 검증 체크리스트

- [ ] **Syntax 오류 확인**: `npm run build` 성공
- [ ] **단위 테스트**: `buildEvidencePack` 함수
  - [ ] 검색 결과 → Evidence Pack 변환
  - [ ] 메타데이터 포함 확인
- [ ] **브라우저 테스트**:
  - [ ] 검색 → Evidence Pack 생성 → Judge 사용
  - [ ] 점수 분해 정보 확인
- [ ] **기존 기능 정상 동작**:
  - [ ] P0 + Phase 1-3 정상

---

## 📊 P1 전체 완료 검증

### 최종 체크리스트

- [ ] **빌드 성공**: `npm run build` 에러 없음
- [ ] **DB 마이그레이션 완료**: `telemetry_logs` 테이블
- [ ] **전체 플로우 테스트**:
  - [ ] 모드 선택 → 라우팅 → 응답
  - [ ] Reviewer 배지 표시
  - [ ] Telemetry 로그 저장
  - [ ] Evidence Pack 활용
- [ ] **성능 테스트**:
  - [ ] cheap 모드 < 3초
  - [ ] standard 모드 < 5초
  - [ ] strict 모드 < 10초
- [ ] **Vercel 배포 확인**
- [ ] **P0 회귀 테스트**: P0 기능 모두 정상

---

## 📝 품질 기준 요약

| 기준                 | 확인 방법              |
| -------------------- | ---------------------- |
| 코딩 스타일 일치     | ESLint 통과            |
| 명확한 함수명/변수명 | 코드 리뷰              |
| 에러 처리 존재       | try-catch, 에러 메시지 |
| 성능 이슈 없음       | 비동기 로깅, 타임아웃  |
| 접근성 고려          | aria-label, role 속성  |

---

## 📌 다음 단계 (P2)

P1 완료 후 필요 시 진행:

- [ ] Hard Negative 수집 시스템
- [ ] 모드 프롬프트 (코칭/교정/채점)
- [ ] Category/Difficulty 태깅
